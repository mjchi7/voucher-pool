package com.boost.voucherpool.constant;

import java.math.BigDecimal;

public class Parameter {

    // The maximum discount percentage allowed
    public static final BigDecimal MAX_DISCOUNT_PCT = BigDecimal.valueOf(100.0);

    // The minimum discount percentage allowed
    public static final BigDecimal MIN_DISCOUNT_PCT = BigDecimal.valueOf(100.0);
}
